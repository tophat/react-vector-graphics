{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna exec tsc",
    "build:watch": "lerna exec tsc --parallel -- --watch",
    "commit": "git-cz",
    "lint": "eslint packages/*/src/*.ts",
    "lint:fix": "eslint packages/*/src/*.ts --fix",
    "lock:check": "yarn-deduplicate --list",
    "lock:fix": "yarn-deduplicate",
    "release": "lerna-semantic-release pre && lerna-semantic-release post && lerna-semantic-release perform",
    "report:coverage": "codecov",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:modified": "jest --bail --findRelatedTests",
    "test:watch": "jest --watch --notify"
  },
  "devDependencies": {
    "@tophat/commitizen-adapter": "^0.0.11",
    "@tophat/commitlint-config": "^0.1.2",
    "@tophat/eslint-config": "^0.3.1",
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.9",
    "@typescript-eslint/eslint-plugin": "^2.7.0",
    "@typescript-eslint/parser": "^2.8.0",
    "codecov": "^3.6.1",
    "commitizen": "^4.0.3",
    "commitlint": "^8.2.0",
    "eslint": "^6.6.0",
    "eslint-config-prettier": "^6.6.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-jest": "^23.1.1",
    "eslint-plugin-jsx-a11y": "^6.2.3",
    "eslint-plugin-prettier": "^3.1.1",
    "eslint-plugin-react": "^7.17.0",
    "husky": "^3.0.9",
    "jest": "^24.9.0",
    "lerna": "^3.18.4",
    "lerna-semantic-release": "^9.1.0",
    "lint-staged": "^9.4.3",
    "prettier": "^1.19.1",
    "ts-jest": "^24.1.0",
    "typescript": "^3.7.2",
    "yarn-deduplicate": "1.1.1"
  },
  "jest": {
    "preset": "ts-jest",
    "moduleDirectories": [
      "./packages/*/src/",
      "./packages/*/tests/",
      "./node_modules"
    ],
    "testPathIgnorePatterns": [
      "./artifacts/",
      "./node_modules/"
    ],
    "coverageDirectory": "./artifacts/coverage"
  },
  "commitlint": {
    "extends": [
      "@tophat/commitlint-config"
    ]
  },
  "config": {
    "commitizen": {
      "path": "@tophat/commitizen-adapter"
    }
  },
  "lint-staged": {
    "*.{js,ts}": [
      "yarn lint",
      "yarn test:modified"
    ],
    "yarn.lock": [
      "yarn lock:check"
    ]
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "lint-staged"
    }
  }
}
