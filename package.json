{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna exec tsc",
    "watch": "lerna exec tsc -- --watch"
  },
  "devDependencies": {
    "@types/node": "^12.0.10",
    "lerna": "^3.15.0",
    "typescript": "^3.5.2"
  }
}
